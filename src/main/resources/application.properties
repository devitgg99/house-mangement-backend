spring.application.name=pvhcenima_api
# Database
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-wispy-shadow-a415aqxv.us-east-1.aws.neon.tech:5432/neondb?sslmode=require&channel_binding=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_rQZ0TckC2vlU}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# JWT
jwt.key=${JWT_KEY}
jwt.access-token-expiration=3600000
jwt.refresh-token-expiration=86400000
# AWS S3 (use environment variables - NEVER commit secrets!)
cloud.aws.credentials.access-key=${AWS_ACCESS_KEY}
cloud.aws.credentials.secret-key=${AWS_SECRET_KEY}
cloud.aws.region.static=${AWS_REGION:ap-southeast-2}
cloud.aws.s3.bucket=${S3_BUCKET:housemanagentbk}
# Utility Settings
utility.water-price-per-unit=0.5
